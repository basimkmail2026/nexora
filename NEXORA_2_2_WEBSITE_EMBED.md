# Nexora 2.2 Website Embed

هذه النسخة تضيف نظام ربط المساعدين بالمواقع الخارجية بشكل متكامل:

- مفتاح Widget عام مستقل لكل مساعد.
- كود تضمين جاهز من لوحة المساعد.
- تحديد الدومينات المسموحة وحماية النطاق.
- نافذة محادثة عائمة متجاوبة مع الهاتف.
- مظهر فاتح/داكن/تلقائي ولون وموضع قابلان للتعديل.
- رسالة ترحيب ونص زر ونص مربع كتابة قابل للتعديل.
- جمع اسم وبريد الزائر بشكل اختياري.
- حفظ موقع الزائر والصفحة والمحادثة داخل لوحة Nexora.
- صفحة محادثات الزوار مع عرض الرسائل وتغيير الحالة.
- ربط الودجت بقاعدة معرفة المساعد وFAQ.
- API Key مستقل للتكاملات البرمجية.

## طريقة الاستخدام

1. افتح المساعد وانشره.
2. افتح تبويب "ربط المواقع".
3. أضف دومين الموقع، مثال: `example.com`.
4. احفظ الإعدادات.
5. انسخ كود التضمين وضعه قبل `</body>` في الموقع الآخر.
6. افتح تبويب "محادثات الزوار" لمشاهدة الرسائل.

Render يشغّل `npm run db:prepare` عند بدء الخدمة، ولذلك ستُضاف حقول Prisma الجديدة تلقائيًا.
